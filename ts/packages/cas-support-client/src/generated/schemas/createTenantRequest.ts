/**
 * Generated by orval v8.10.0 🍺
 * Do not edit manually.
 * OneNexus Central Auth Service — Support API
 * Support-team JSON RPC operations under `/support-api/*`. OAuth 2.1 / OIDC protocol endpoints under `/connect/*` are documented separately by their respective specs and the OIDC discovery document at `/.well-known/openid-configuration`.
 * OpenAPI spec version: v1
 */

/**
 * Request body for `POST /support-api/CreateTenant`.
 */
export interface CreateTenantRequest {
  /**
     * Globally unique, immutable slug that members use to locate the tenant
  when they sign in (for example, `acme`).
     * @minLength 1
     * @maxLength 54
     * @pattern ^(?!(?:onenexus(?:-|$)|platform$|system$|admin$))[a-z0-9][a-z0-9_-]{0,53}$
     */
  tenantSlug: string;
  /**
     * Human-readable display name (e.g. `"Acme Corporation, Inc."`).
  1–200 chars. Not used for auth decisions; shown in admin UIs only.
     * @minLength 1
     * @maxLength 200
     */
  displayName: string;
  /**
     * Email address of the tenant's root user. Tenant creation sends this
  user an activation invitation and grants the predefined
  `TenantAdmin` role before activating the tenant.
     * @minLength 3
     * @maxLength 254
     */
  rootEmail: string;
  /**
     * Display name of the tenant's root user.
     * @minLength 1
     * @maxLength 200
     */
  rootDisplayName: string;
  /**
     * Idempotency key bound to a hash of this complete onboarding request.
  A retry with the same token and content resumes unfinished stages;
  changing the content or tenant behind a token is rejected.
     * @minLength 16
     * @maxLength 128
     * @pattern ^[a-zA-Z0-9_.-]+$
     */
  clientToken: string;
}
