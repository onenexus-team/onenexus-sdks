/**
 * Generated by orval v8.10.0 🍺
 * Do not edit manually.
 * OneNexus Central Auth Service — Support API
 * Support-team JSON RPC operations under `/support-api/*`. OAuth 2.1 / OIDC protocol endpoints under `/connect/*` are documented separately by their respective specs and the OIDC discovery document at `/.well-known/openid-configuration.
 * OpenAPI spec version: v1
 */
import type { CedarDiagnosticEntityAttributes } from './cedarDiagnosticEntityAttributes';
import type { CedarDiagnosticEntityTags } from './cedarDiagnosticEntityTags';
import type { CedarDiagnosticEntityUid } from './cedarDiagnosticEntityUid';

/**
 * One entity supplied to Cedar for a privileged support authorization
evaluation. The list is the dynamic entity slice CAS passes to Cedar: the
evaluated principal, resource, and every relationship entity needed by the
decision. Cedar derives static action entities from its packaged schema,
so they are not repeated here.
 */
export interface CedarDiagnosticEntity {
  /** The entity's Cedar UID. */
  uid: CedarDiagnosticEntityUid;
  /** Entity attributes keyed by their Cedar attribute name. */
  attributes: CedarDiagnosticEntityAttributes;
  /** The entity's direct parent UIDs. */
  parents: CedarDiagnosticEntityUid[];
  /** Native Cedar tags keyed by tag name. */
  tags: CedarDiagnosticEntityTags;
}
