/**
 * Generated by orval v8.10.0 🍺
 * Do not edit manually.
 * OneNexus Central Auth Service — Support API
 * Support-team JSON RPC operations under `/support-api/*`. OAuth 2.1 / OIDC protocol endpoints under `/connect/*` are documented separately by their respective specs and the OIDC discovery document at `/.well-known/openid-configuration.
 * OpenAPI spec version: v1
 */
import type { TenantSummary } from './tenantSummary';

/**
 * Response body for `POST /support-api/ListTenants`.
 */
export interface ListTenantsResponse {
  /** Tenant snapshots for the current page, always ordered by
  Guid TenantSummary.TenantId ascending. Because the PK is
  UUID v7 — a time-ordered identifier — this is the natural
  creation-order listing, with no separate `created_at` index
  needed. */
  items: TenantSummary[];
  /**
     * Cursor for the previous page. `null` when no earlier items
  exist.
     * @nullable
     */
  before?: string | null;
  /**
     * Cursor for the next page. `null` when no later items exist.
     * @nullable
     */
  after?: string | null;
}
