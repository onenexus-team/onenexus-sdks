/**
 * Generated by orval v8.10.0 🍺
 * Do not edit manually.
 * OneNexus Central Auth Service — Customer API
 * Customer-facing JSON RPC operations under `/api/*`. OAuth 2.1 / OIDC protocol endpoints under `/connect/*` are documented separately by their respective specs and the OIDC discovery document at `/.well-known/openid-configuration`.
 * OpenAPI spec version: v1
 */

/**
 * Wire shape for a user returned by the `CreateUser` family of RPCs.
 */
export interface User {
  /** UUID v7 primary key. */
  userId: string;
  /** Canonical principal URI used when assigning authorization roles to
  this user. */
  userUri: string;
  /** Owning tenant's UUID v7 primary key. */
  tenantId: string;
  /** The user's email (verbatim, not normalised). */
  email: string;
  /** Display name shown in UIs. */
  displayName: string;
  /** Whether the user has accepted the invitation and verified their inbox.
  `false` until `AcceptInvitation` succeeds. */
  emailConfirmed: boolean;
  /** UTC instant when the row was created. */
  createdAt: string;
}
