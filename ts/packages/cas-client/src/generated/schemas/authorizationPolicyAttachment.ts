/**
 * Generated by orval v8.10.0 🍺
 * Do not edit manually.
 * OneNexus Central Auth Service — Customer API
 * Customer-facing JSON RPC operations under `/api/*` and authenticated account self-service under `/account/*`. OAuth 2.1 / OIDC protocol endpoints under `/connect/*` are documented separately by their respective specs and the OIDC discovery document at `/.well-known/openid-configuration`.
 * OpenAPI spec version: v1
 */
import type { AuthorizationPolicyReference } from './authorizationPolicyReference';

/**
 * One policy-to-role attachment returned by CAS.
 */
export interface AuthorizationPolicyAttachment {
  /** Stable identifier of this direct policy-to-role attachment. */
  attachmentId: string;
  /** Policy attached to the role. */
  policy: AuthorizationPolicyReference;
  /** URI of the role that receives the policy. */
  roleUri: string;
  /** Concurrency token required to remove this attachment. */
  stateToken: string;
  /** URI of the principal that made the attachment. */
  attachedByUri: string;
  /** When the attachment was created, in UTC. */
  attachedAtUtc: string;
}
